apiVersion: v1
kind: Service
metadata:
  name: music-player-service
spec:
  type: NodePort
  selector:
    app: Music-Player
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 31000
